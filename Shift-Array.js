// the array shift method removes the first element
//  from an array and returnes it. we can think of it as the opposite
// of the pop method .


let numbers = [1,2,3,4];

let removed = numbers.shift();

console.log(numbers);

console.log(removed); // it will return the  removed number.
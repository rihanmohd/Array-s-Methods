// the array indexOf method returns the first index at which a given element 
// can be found inside array .


// let names = ['zaid', 'nadeem', 'rihan'];
// console.log(names.indexOf('zaid'));






//for exp we want to swap out something from the array but we first to need to know
//  what index is it ,


// let names = ['zaid', 'nadeem', 'rihan'];

// let idx = names.indexOf('zaid');

// names[idx] = 'ahmad';

// console.log(names);







// we can do something fun like 

// let names = ['zaid', 'nadeem', 'rihan'];

// let idxx = names.indexOf('zaid');

// if(idxx > -1){
//     console.log("we have this");

// }else{

//     console.log("not given");
// };










// what will happen if we have multiple event , the indexOf method returns the first index of 
// element it finds inside the array .

// let name = ['a', 'b', 'c','a', 'c'];

// let idx = name.indexOf('c');

// console.log(idx);





// we have also the last IndexOf array method which guess what it will return 
// the last index from the end of the array.

let name = ['a', 'b', 'c','a', 'c'];

let idx = name.lastIndexOf('c');

console.log(idx);


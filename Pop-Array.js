//  the array pop method will remove the last item from an array 
// and it will return  it.

let numbers = [1,2,3,4,5];

 let last = numbers.pop();  // we can call it multiple times.

console.log(numbers);

console.log(last); // it will also return it .


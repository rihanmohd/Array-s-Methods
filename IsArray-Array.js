//  the java script isArray method chacks if the passed value is an array.
// this  isArray method uses only Array class.
let names = ['a', 'b', 'c', 'd'];

let str = 'Hello world';

let number = 20;

console.log(Array.isArray(names));

console.log(Array.isArray(str));

console.log(Array.isArray(number));
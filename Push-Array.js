// the array push method allows us to add one or more elements at the end of the 
// array and it also returns the new length of the array.


let numbers = [1,2,3,4,5,6];
let total = numbers.push(5, 4);    // we can pass here multiple parametrs.
console.log(numbers);

console.log(total);   // it will return the total length of the new array.
